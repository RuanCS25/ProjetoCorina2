@model IEnumerable<ProjetoCorina2.Models.RegPresenca>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Alunos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataPresenca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Refeicao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Alunos.AlunoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataPresenca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Refeicao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RegPresencaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RegPresencaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RegPresencaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
