@model IEnumerable<ProjetoCorina2.Models.RegistroAusencia>

@{
    ViewData["Title"] = "Index";
}

<h1>Registro Ausencia</h1>

<p>
    <a asp-action="Create" class="btn btn-secondary">Criar Novo</a>
</p>
<table class="table table-dark table-striped ">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Aluno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Refeicao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Aluno.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Data)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Refeicao)
                </td>
                <td>
                    <div class="btn-group">
                        <a asp-action="Edit" asp-route-id="@item.RegistroAusenciaId" class="btn btn-outline-secondary">Editar</a>
                        <a asp-action="Details" asp-route-id="@item.RegistroAusenciaId" class="btn btn-outline-secondary">Detalhes</a>
                        <a asp-action="Delete" asp-route-id="@item.RegistroAusenciaId" class="btn btn-outline-danger">Deletar</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
